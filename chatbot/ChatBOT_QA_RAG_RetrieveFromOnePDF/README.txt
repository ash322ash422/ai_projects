- I used python3.11
- I installed packages in following order:
pip install databutton langchain pypdf openai streamlit 
pip install -U langchain-community 
pip install faiss-cpu   
pip install openai==0.28

( or use requirements.txt) 


- To run:
> streamlit run .\app.py 

- Goto browser and load a PDF doc. Ask any questions related with the PDF doc and it would give answer.

